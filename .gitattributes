# 统一行尾为 LF，避免 Windows/macOS/Linux 协作时产生无意义的行尾差异
* text=auto eol=lf

# 显式声明二进制类型（防止被当成文本处理）
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.zip  binary
*.woff binary
*.woff2 binary
*.ttf  binary
